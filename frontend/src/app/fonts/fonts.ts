import { Crimson_Text, Delius } from "next/font/google";

export const crimsonText = Crimson_Text({
  weight: "400",
  subsets: ["latin"],
});

export const delius = Delius({
  weight: "400",
  subsets: ["latin"],
});
